# 快速开始 - 部署到 GitHub Pages

## 🎯 5 分钟快速部署

### 步骤 1: 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称：`avatar-generator`（或你喜欢的名称）
3. 选择 Public
4. **不要**勾选任何初始化选项
5. 点击 Create repository

### 步骤 2: 更新配置文件

**编辑 `vite.config.ts`**：
```typescript
// 将 '/avatar-generator/' 改为你的仓库名称
base: process.env.NODE_ENV === 'production' ? '/你的仓库名/' : '/',
```

**编辑 `package.json`**：
```json
"homepage": "https://你的GitHub用户名.github.io/你的仓库名"
```

### 步骤 3: 推送代码

在项目目录运行：

```bash
# 添加远程仓库（替换为你的实际地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 重命名分支为 main
git branch -M main

# 推送代码
git push -u origin main
```

### 步骤 4: 启用 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. 在 **Source** 部分选择 **GitHub Actions**
5. 保存设置

### 步骤 5: 等待部署

- GitHub Actions 会自动开始部署
- 查看 **Actions** 标签页可以看到部署进度
- 部署完成后，访问 `https://你的用户名.github.io/你的仓库名`

## ✅ 完成！

你的头像生成器现在已经在线了！

## 🔄 更新网站

以后每次推送代码到 `main` 分支，GitHub Actions 会自动重新部署。

```bash
git add .
git commit -m "更新内容"
git push
```

## 🆘 遇到问题？

- 查看 [DEPLOY.md](./DEPLOY.md) 了解详细部署指南
- 检查 GitHub Actions 的部署日志
- 确保 `vite.config.ts` 中的 `base` 路径正确

